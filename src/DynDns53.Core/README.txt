
        // Get current IP [using a plugin]  --> MEF

		// DNS adapter --> MEF [AWS or some other API that manages domains]

        // Get recorded IP for record [home1.volki.info]
        // Get recorded IP for record [home2.volki.info]

        // update all recorded subdomains

        // run every n minutes
        // start on system startup and hide to tray

        // Logo DD53


        // Settings:
        // AWS keys, domain names, interval